name: save

on:
  push:
    branches:
      - main
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-latest
    timeout-minutes: 5

    steps:
      - name: Setup SSH key
        env:
          SSH_KEY: ${{ secrets.SSH_KEY }}
        run: |
          mkdir -p ~/.ssh
          echo "${SSH_KEY}" > ~/.ssh/id_rsa
          chmod 400 ~/.ssh/id_rsa
          ssh-keyscan github.com >> ~/.ssh/known_hosts
      - name: checkout
        uses: actions/checkout@v2
      - name: Commit kawamachi
        run: |
          git clone git@github.com:labeneko/kawamachi.git output
          TZ=Asia/Tokyo date '+%Y%m%d%H%M' > output/time.txt
          git config --local user.email "info@laboneko.jp"
          git config --local user.name "labeneko"
          git add .
          git commit -m "時間を保存" -a || true
      - name: Pushes to kawamachi
        uses: cpina/github-action-push-to-another-repository@main
        env:
          API_TOKEN_GITHUB: ${{ secrets.TOKEN }}
        with:
          source-directory: 'output'
          destination-github-username: 'labeneko'
          destination-repository-name: 'kawamachi'
          user-email: info@laboneko.jp
          target-branch: main
      - name: Commit daimaru
        run: |
          git clone git@github.com:labeneko/daimaru.git output
          TZ=Asia/Tokyo date '+%Y%m%d%H%M' > output/time.txt
          git config --local user.email "info@laboneko.jp"
          git config --local user.name "labeneko"
          git add .
          git commit -m "時間を保存" -a || true
      - name: Pushes to daimaru
        uses: cpina/github-action-push-to-another-repository@main
        env:
          API_TOKEN_GITHUB: ${{ secrets.TOKEN }}
        with:
          source-directory: 'output'
          destination-github-username: 'labeneko'
          destination-repository-name: 'daimaru'
          user-email: info@laboneko.jp
          target-branch: main
